# Dependencies are declared once, in pyproject.toml.
# This file exists only so `pip install -r requirements.txt` keeps working:
# it installs the backend package (plus dev/test tools) in editable mode.
# Run it from the backend/ directory: pip install -r requirements.txt
-e .[dev]
